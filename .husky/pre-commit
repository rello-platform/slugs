#!/usr/bin/env sh
# Pre-commit: fast typecheck (full tsc + build in pre-push)
# Generated by PLATFORM-RETIRE-GH-ACTIONS-LOCAL-HUSKY-MIGRATION 2026-05-18.
# Rello-Slugs has no test script today (per discovery matrix L455), so the
# husky-init default `npm test` would fail; replaced with the minimal-fallback
# pattern from the Journey-Engine canary.

set -e

echo "[pre-commit] Running tsc --noEmit..."
npx tsc --noEmit

echo "[pre-commit] OK."
