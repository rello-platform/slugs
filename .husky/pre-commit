#!/usr/bin/env sh
# Pre-commit: fast typecheck (full tsc + build in pre-push)
# Generated by PLATFORM-RETIRE-GH-ACTIONS-LOCAL-HUSKY-MIGRATION 2026-05-18.
# Kept typecheck-only for speed. A `test` script DOES exist as of v0.6.0
# (`tsc -p tsconfig.test.json --noEmit && vitest run`); pre-push runs it via
# its conditional test block, so it is deliberately not duplicated here.

set -e

echo "[pre-commit] Running tsc --noEmit..."
npx tsc --noEmit

echo "[pre-commit] OK."
