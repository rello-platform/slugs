{
  "name": "@rello-platform/slugs",
  "version": "0.8.0",
  "description": "Canonical platform slug registry for the Rello ecosystem — consumer apps + service engines.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "compile": "tsc",
    "test": "tsc -p tsconfig.test.json --noEmit && vitest run",
    "prepublishOnly": "npm run compile",
    "hooks": "husky || true; git config core.hooksPath .husky || true"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "typescript": "^5.4.0",
    "vitest": "^4.1.10"
  },
  "repository": {
    "type": "git",
    "url": "github:rello-platform/slugs"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "license": "UNLICENSED"
}
